[package]
name = "agentguard-auditor"
version = "0.1.0"
edition = "2024"
description = "Static security auditor for MCP tool manifests"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
